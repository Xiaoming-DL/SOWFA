      sliceDataInstantaneous
      {
          type                 surfaces;
          functionObjectLibs   ("libsampling.so");
          enabled              true;
          interpolationScheme  cell;
          outputControl        timeStep;
          outputInterval       10;
          surfaceFormat        vtk;
          fields
          (
              U
              p

          );
          surfaces
          (
              slice_horizontal
              {
                  type         plane;
                  basePoint    (1500.00 1000.00 90);
                  normalVector (0 0 1);
                  triangulate  false;
              }

              slice_cross_stream
              {
                  type         plane;
                  basePoint    (1500.00 1000.00 90);
                  normalVector (0 1 0);
                  triangulate  false;
              }

              slice_streamwise
              {
                  type         plane;
                  basePoint    (1500.00 1000.00 90);
                  normalVector (1 0 0);
                  triangulate  false;
              }
          );
      }
